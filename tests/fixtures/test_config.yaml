api:
  base_url: "https://api.openweathermap.org/data/2.5/"
  timeout: 10
  units: "metric"

defaults:
  city: "Tokyo"
  language: "ja"

web:
  host: "127.0.0.1"
  port: 5000
  debug: true

logging:
  level: "ERROR"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"